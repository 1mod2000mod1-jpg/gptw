services:
  - type: web
    name: telegram-bot-guaranteed
    env: python
    plan: free
    branch: main
    buildCommand: pip install -r requirements-guaranteed.txt
    startCommand: python app.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: PYTHON_VERSION
        value: 3.10.12
